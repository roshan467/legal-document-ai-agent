python-docx==1.2.0
streamlit==1.63.0
pytest==9.1.1
# anthropic is optional — only needed if you enable the LLM-polish bonus
# feature (Stage 4b). The pipeline runs completely without it.
anthropic>=0.34.0
