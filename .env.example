# Optional. Only needed if you want to enable the LLM-polish bonus feature
# (Stage 4b in src/llm_polish.py), which lightly smooths substantive-answer
# paragraph phrasing using Claude. The core pipeline (extraction, mapping,
# generation, evaluation) works fully without this — it uses zero API keys
# by design, per the assignment's note that free tiers are fine and no one
# should have to pay for API access.
ANTHROPIC_API_KEY=
